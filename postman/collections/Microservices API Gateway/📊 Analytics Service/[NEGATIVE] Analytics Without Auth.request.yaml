$kind: http-request
url: "{{baseUrl}}/api/v1/analytics/events/{{eventId}}"
method: GET
scripts:
  - type: afterResponse
    code: pm.test("Status is 401 Unauthorized", () =>
      pm.response.to.have.status(401));
    language: text/javascript
order: 6000
